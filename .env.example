# Local development placeholders only.
# Never commit real secrets or API keys.

# Optional future server-side AI integration.
# The recovered prototype imported in this repo does not require this value.
OPENAI_API_KEY=
