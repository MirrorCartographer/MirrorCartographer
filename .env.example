OPENAI_API_KEY=replace_with_your_key_in_local_or_hosting_env_only

# Do not commit .env.local.
# Do not paste real keys into chat, screenshots, GitHub, docs, or downloadable bundles.
# The first reflection loop currently uses local symbol-table logic and does not require an API key.
