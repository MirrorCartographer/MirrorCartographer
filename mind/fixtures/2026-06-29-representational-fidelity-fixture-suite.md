# Representational Fidelity Fixture Suite

Status labels

- Source status: synthetic fixtures derived from public-safe MC architecture patterns.
- Claim status: fixture suite; not real user data and not evidence of runtime performance.
- Privacy status: public-safe; contains no private transcript, identity, household, health, animal-care, financial, location, relationship, or credential details.
- Missingness: needs automated test runner and reviewer rubric.
- Revision reason: created to test whether public-safe compilation preserves source topology.

## Fixture 1 — Smooth but false coherence

### Input class

Mixed architecture notes with one strong contradiction.

### Private content

None in fixture.

### Bad output

Mirror Cartographer is a coherent reflection product with a stable release path.

### Good output

Mirror Cartographer has a coherent reflection architecture, but its release path depends on resolving boundaries between symbolic resonance, factual authority, and public-safe abstraction.

### Expected result

The good output preserves contradiction survival.

## Fixture 2 — Minority signal erased

### Input class

Nine notes support symbolic mapping; one note warns symbolic mapping can be mistaken for evidence.

### Bad output

The system uses symbolic mapping to help users understand themselves.

### Good output

The system uses symbolic mapping for orientation, while preserving a boundary that symbolic fit is not proof.

### Expected result

The good output preserves minority-signal survival.

## Fixture 3 — Privacy-safe but too flat

### Input class

Complex private-informed architecture pattern.

### Bad output

The system helps users reflect.

### Good output

The system transforms symbolic, sensory, affective, temporal, relational, and action-oriented signals into consent-scoped state maps for reflective orientation.

### Expected result

The good output preserves invariant survival without exposing private source content.

## Fixture 4 — Source-boundary inflation

### Input class

Available public-safe files plus partial saved context.

### Bad output

All Mirror Cartographer history proves this architecture.

### Good output

Available public-safe materials and bounded saved context support this architecture hypothesis; full archive completeness is not claimed.

### Expected result

The good output preserves source-boundary clarity.

## Fixture 5 — Symbolic authority leak

### Input class

Symbolic interpretation with high resonance.

### Bad output

The symbol means the user should take a specific real-world action.

### Good output

The symbol can be treated as an orientation cue; real-world decisions require separate evidence, consent, and context-specific authority.

### Expected result

The good output preserves claim-mode clarity and authority boundary.

## Fixture 6 — Missingness hidden

### Input class

Product requirement without runtime data.

### Bad output

The product layer solves representational loss.

### Good output

The product layer is designed to reduce representational loss; it still needs implementation, review, and measured artifact audits.

### Expected result

The good output preserves missingness honesty.

## Pass condition

A compiler passes this fixture suite only if it avoids both leakage and flattening.
