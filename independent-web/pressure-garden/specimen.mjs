function esc(value){return String(value).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]))}
function n(value,digits=2){return Number(value).toFixed(digits)}
export function specimenId(genome){if(!genome||!Number.isInteger(genome.seed))throw new TypeError('genome with integer seed required');return `pg-${genome.seed.toString(16).padStart(8,'0')}`}
export function specimenSVG(genome,{parents=[]}={}){if(!genome||!Number.isInteger(genome.seed))throw new TypeError('genome with integer seed required');const id=specimenId(genome),h=genome.hue,height=110*genome.height,stemX=160+genome.curl*18,root=24+genome.root*26,branches=Math.max(2,Math.min(7,genome.branches)),petals=Math.max(2,Math.min(10,genome.bloom));let branch='';for(let i=1;i<=branches;i++){const k=i/(branches+1),y=236-height*k,side=i%2?1:-1,len=24+(1-k)*25;branch+=`<path d="M ${n(160+(stemX-160)*k)} ${n(y)} q ${n(side*len*.45)} -${n(len*.2)} ${n(side*len)} -${n(len*.06)}"/>`}let bloom='';for(let i=0;i<petals;i++){const a=(Math.PI*2*i)/petals,px=stemX+Math.cos(a)*16,py=236-height+Math.sin(a)*9;bloom+=`<ellipse cx="${n(px)}" cy="${n(py)}" rx="13" ry="5" transform="rotate(${n(a*180/Math.PI)} ${n(px)} ${n(py)})"/>`}const lineage=parents.length===2?`${parents[0].toString(16)} × ${parents[1].toString(16)}`:'wild';return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 320 420" role="img" aria-labelledby="t d"><title id="t">Pressure Garden specimen ${id}</title><desc id="d">A deterministic pressed botanical drawing generated from visible genome traits.</desc><rect width="320" height="420" fill="#efe8ce"/><g fill="none" stroke="hsl(${h} 38% 29%)" stroke-linecap="round"><path stroke-width="3" d="M160 236 Q ${n(154+genome.curl*9)} ${n(236-height*.52)} ${n(stemX)} ${n(236-height)}"/>${branch}<path stroke-width="1.6" d="M160 236 q -18 20 -${n(root)} ${n(root*1.35)} M160 236 q 18 20 ${n(root)} ${n(root*1.35)} M160 236 q 0 22 0 ${n(root*1.6)}"/></g><g fill="hsl(${(h+28)%360} 58% 66%)" opacity=".9">${bloom}<circle cx="${n(stemX)}" cy="${n(236-height)}" r="6" fill="hsl(${(h+342)%360} 52% 42%)"/></g><g fill="#243129" font-family="ui-monospace,monospace"><text x="24" y="34" font-size="11" letter-spacing="2">PRESSURE GARDEN / PRESSED</text><text x="24" y="365" font-size="14">${esc(id)}</text><text x="24" y="386" font-size="10">lineage ${esc(lineage)}</text><text x="24" y="402" font-size="9">h${h} · ht${n(genome.height)} · b${branches} · fl${petals} · c${n(genome.curl)} · r${n(genome.root)}</text></g></svg>`}
