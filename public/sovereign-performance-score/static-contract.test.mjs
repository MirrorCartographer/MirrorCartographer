import fs from 'node:fs';import assert from 'node:assert/strict';const h=fs.readFileSync(new URL('./index.html',import.meta.url),'utf8');const s=JSON.parse(fs.readFileSync(new URL('./interaction-evidence.schema.json',import.meta.url),'utf8'));const checks=[['no autoplay',!/autoplay/i.test(h)],['explicit consent',h.includes('Wake sound')],['reduced motion',h.includes('prefers-reduced-motion')],['keyboard',h.includes('ArrowRight')&&h.includes("e.key==='Enter'")],['touch safe',h.includes('min-height:44px')],['local first',h.includes('localStorage')],['offline',h.includes('serviceWorker')],['bounded evidence',h.includes('slice(-180)')],['bounded history',h.includes('slice(-40)')],['strict schema',s.additionalProperties===false&&s.properties.events.items.additionalProperties===false],['no microphone',!/getUserMedia|MediaRecorder/.test(h)],['no analytics',!/analytics|sendBeacon|fetch\(['\"]https?:/.test(h)],['export evidence',h.includes('Export approved evidence')],['erase evidence',h.includes('Erase evidence')],['erase all',h.includes('Erase all local state')],['import validation',h.includes('fia.composition.memory-export/1')],['accessible transcript',h.includes('Accessible score transcript')],['no raw import retention',h.includes("$('importBox').value='' ".trim())]];for(const[c,v]of checks){assert.ok(v,c);console.log('PASS',c)}console.log(`\nsovereign performance static contract: ${checks.length} checks passed`)